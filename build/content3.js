// Section 7 (Configuration Reference) and Section 8 (Bug fixes record).

const SEC7_CONFIG = [
  { h1: '7. Configuration Reference' },
  { h2: '7.1 vRO Action: f5_generate_deployment_name' },
  { table: { headers: ['Setting', 'Value'], widths: [30, 70], rows: [
    ['Module', 'com.f5.automation'],
    ['Return type', 'string'],
    ['Inputs', 'project (string), location (string), environmentType (string) — see 5.2'],
    ['Required Configuration Element', 'Folder "F5-Automation" > element "DeploymentNaming" > attribute "sequenceCounters" (type Properties) — see 5.4'],
  ] } },
  { h2: '7.2 ABX Action: f5_create_virtual_server' },
  { table: { headers: ['Setting', 'Value'], widths: [30, 70], rows: [
    ['Action type', 'Script'],
    ['Runtime', 'Python 3.10'],
    ['Main function', 'handler'],
    ['Dependency', 'requests'],
  ] } },
  { h3: 'Default / Secret Inputs / Action Constants' },
  { table: { headers: ['Name', 'Type', 'Purpose'], widths: [26, 20, 54], rows: [
    ['f5_username', 'Default', 'F5 API username, shared across all requests.'],
    ['F5_SHARED_PASSWORD', 'Secret (must be — see 7.4)', 'F5 API password, shared across all requests.'],
    ['F5_VIP_SUBNET_REGISTRY', 'Action Constant (JSON)', 'Maps each F5 host to its VIP subnet CIDR and reserved addresses — see 6.5. The Value field takes the literal JSON text directly (not a reference to another object) — paste the JSON itself.'],
    ['__probe__', 'Default (optional)', 'Observed in the source environment as an unused leftover from earlier development; grep of the current script confirms it is never read anywhere in the code. Not required for a fresh install — do not create it. If it already exists from a prior deployment, leaving its existing value untouched is safe.'],
    ['username / password', 'Default / Secret (fallback only, not configured)', 'Undocumented fallback keys read by _resolve_credentials() only if f5_username / F5_SHARED_PASSWORD are unset — see Section 6.4. Not part of the standard configuration; do not use these as a substitute for the primary two inputs.'],
  ] } },
  { h2: '7.3 Naming collision to be aware of' },
  { p: 'The string "F5-Automation" appears in two unrelated places in this solution: as the vRA project name in the environment this solution was originally built in, and as the literal, hardcoded vRO Configuration folder name the naming action depends on (Section 5.4). These are independent settings that only happen to share a name in that original environment. A client\'s vRA project can be named anything; the vRO Configuration folder must still be named exactly F5-Automation regardless.' },
  { h2: '7.4 Comparing the configuration mechanisms used' },
  { table: { headers: ['Mechanism', 'Plaintext visible after save?', 'Used here for'], widths: [24, 26, 50], rows: [
    ['Default Input', 'Yes, always', 'f5_username; __probe__ (legacy, unused — see 7.2, do not create fresh)'],
    ['Secret Input', 'No — write-only once saved', 'F5_SHARED_PASSWORD (must be this type)'],
    ['Action Constant', 'Yes, always (not encrypted)', 'F5_VIP_SUBNET_REGISTRY'],
    ['Configuration Element (vRO)', 'Yes, always (not encrypted)', 'sequenceCounters — non-sensitive counter data only'],
  ] } },
  { note: 'Because Secret Inputs are write-only, their value cannot be exported for a backup the way a script or a Default Input can. Plan pre-deployment backups accordingly (Section 9 — Change Management).' },
  { pageBreak: true },
];

const SEC8_BUGFIXES = [
  { h1: '8. The Bug Fixes — What Changed and Why' },
  { h2: '8.1 Bug 1 — Project GUID appearing in the Deployment Name' },
  { h4: 'Symptom' },
  { p: 'Deployment names generated by the Custom Form included a long GUID-looking segment instead of a clean, readable name.' },
  { h4: 'Root cause' },
  { p: 'During earlier troubleshooting, the vRO action\'s Inputs list was accidentally edited in a way that removed the project input\'s declaration while the Custom Form\'s field binding still mapped a "Project" field to an input named project. With the declared input gone, the binding silently fell back to stale/incorrect behavior rather than failing loudly, and the raw project GUID ended up embedded in the output.' },
  { h4: 'Fix applied' },
  { p: 'The project input was restored to the action\'s signature (so the existing Custom Form binding works exactly as before), but the generated name itself was simplified to intentionally exclude project entirely, using only location and environmentType (see Section 5.3). This was a deliberate scope decision — resolving the project GUID to its human-readable display name inside the vRO script proved unreliable, and dropping project from the name entirely was judged the more robust fix.' },
  { h4: 'Verification performed' },
  { ul: [
      'Confirmed via the vRO action editor, after a full browser hard-refresh, that the script and its three inputs persisted correctly on the server (not just visually in a cached page).',
      'Ran the naming action directly with two different location/environment combinations and confirmed clean output with no GUID in either case.',
      'Submitted live test requests through the actual Service Broker request form and confirmed the Deployment Name field showed the expected clean value end-to-end.',
    ],
  },
  { h2: '8.2 Bug 2 — IndentationError in create_monitor(), breaking every ABX run' },
  { h4: 'Symptom' },
  { p: 'Every run of the f5_create_virtual_server ABX action failed immediately with: "expected an indented block after function definition on line 210 (handler.py, line 211)."' },
  { h4: 'Root cause' },
  { p: 'The create_monitor() function\'s docstring was indented at the same level as its own def line, instead of one level deeper as Python requires for a function body:' },
  { code: [
    'BEFORE (broken):',
    '    def create_monitor(self, monitor_name, monitor_type,',
    '                       interval=None, timeout=None):',
    '    """',
    '    Health monitors are never created or modified here -- monitor_type is',
    '    the exact monitor path already selected from the device\'s existing',
    '    monitors (see f5_list_monitors) and is used as-is.',
    '    """',
    '        if not monitor_type:',
    '            return None',
    '        return monitor_type',
  ] },
  { p: 'The docstring lines sat at 4 spaces of indentation — the same as the def line itself — instead of 8 spaces, matching the function body below it (if not monitor_type: at 8 spaces). Python requires the first statement in a function body (including a docstring) to be indented strictly more than the def line; because it was not, Python could not tell where the function body began.' },
  { h4: 'Fix applied' },
  { code: [
    'AFTER (fixed):',
    '    def create_monitor(self, monitor_name, monitor_type,',
    '                       interval=None, timeout=None):',
    '        """',
    '        Health monitors are never created or modified here -- monitor_type is',
    '        the exact monitor path already selected from the device\'s existing',
    '        monitors (see f5_list_monitors) and is used as-is.',
    '        """',
    '        if not monitor_type:',
    '            return None',
    '        return monitor_type',
  ] },
  { p: 'Four spaces of indentation were added to each of the five docstring lines, bringing them to 8 spaces — matching the function body and resolving the error.' },
  { h4: 'Verification performed' },
  { ul: [
      'A minimal local reproduction of both the broken and fixed versions of this function was validated with Python\'s ast.parse before the live editor was touched, confirming the diagnosis and the fix independently.',
      'After applying the fix in the live ABX action editor, the change was confirmed to have persisted server-side.',
      'The entire file (478 lines at the time of this specific fix, not just the fixed function) was reconstructed line-by-line from the live editor and validated as a whole with ast.parse, py_compile, and tokenize — all clean, confirming no other latent syntax errors existed anywhere else in the file.',
    ],
  },
  { h2: '8.3 Hardening applied during packaging (beyond the two reported bugs)' },
  { p: 'An independent adversarial review of the full deployment package (both scripts and both documents) was performed before this SOP and its companion Deployment Guide were finalized. The following low-risk, non-behavior-changing improvements were made directly to f5_create_virtual_server.py as a result — they do not change what the script does on the happy path, only how clearly it reports problems:' },
  { ul: [
      'Required request fields (host, vs, pool, vs.name, pool.name, vs.port/vs.ports) now raise a clear F5OperationError naming the missing field, instead of an opaque KeyError.',
      'A malformed F5_VIP_SUBNET_REGISTRY value now raises a clear, actionable F5OperationError instead of an unhandled json.JSONDecodeError.',
      'The credential-check error now raises the same F5OperationError type as every other error path in the script (it previously raised a bare RuntimeError).',
      'The module docstring\'s description of virtual-server output naming was corrected to match actual behavior (the -<port> suffix is only added when more than one port is requested).',
      'The vRO action\'s header comment, which still described the old <Project>-<Location>-<EnvironmentType>-<seq> format from before the Bug 1 fix, was corrected to describe the actual current output format.',
      'The module docstring and two in-code comments that described a per-cluster credential map (F5_CREDENTIAL_MAP, F5_LOCATION1_PASSWORD / F5_LOCATION2_PASSWORD, F5_LOCATION_SUBNETS) were corrected — none of those names exist anywhere in the code. The comments now accurately describe the single shared f5_username / F5_SHARED_PASSWORD credential pair the code actually uses (see Section 6.4 and Section 12, item 1). This comment-only correction, plus the council review pass described below, brought the file from 478 to 486 lines.',
    ],
  },
  { p: 'All three syntax-level validation methods (ast.parse, py_compile, tokenize) were re-run after every change described in this section, including the credential-comment correction above, and remain clean. A line-by-line diff confirmed the vRO action\'s executable logic is byte-for-byte identical to what was verified live in the source environment — only comments and one whitespace-only line were changed.' },
  { h2: '8.4 A second, independent council review before delivery' },
  { p: 'Before this package was finalized, four independent reviewers were run against both documents and both scripts: an L1-usability reviewer (could a Tier-1 engineer with no vRA/vRO/F5 background actually follow the Deployment Guide?), a completeness/glossary reviewer (does the SOP define every term it uses and document every feature the code has?), a technical-accuracy reviewer (does every code excerpt, table, and claim match the real scripts exactly?), and a skeptical reviewer instructed to look specifically for contradictions between the two documents, unsafe defaults presented too casually, and the single most likely way a real engineer gets this wrong under time pressure.' },
  { p: 'All findings from that review that were genuinely just clarity, cross-reference, or documentation-completeness gaps were fixed directly in this SOP and the Deployment Guide (the corrected cross-references in the Guide, the newly-documented create_monitor() interval/timeout no-op behavior in Section 6.7, the synchronized seven-item risk list in Section 12 and the Guide\'s Section 8, the proactive Configuration Element setup step now in Guide Section 3, the per-item sign-off table in Section 15, and the additional glossary terms in Section 3, among others). Two findings were judged to require an actual owner decision rather than a documentation fix, and are recorded as Section 12 items 1 and 3 (the shared-credential model is a design limitation, not a typo; TLS verification has no configuration lever in this package today) — these are flagged for the client\'s automation owner, not silently resolved.' },
  { p: 'Findings from that same review that would change runtime behavior or security posture — rather than just clarity — were deliberately not silently fixed, and are documented in Section 12 instead, for the client\'s F5/automation owner to decide on explicitly.' },
  { h2: '8.5 Round 2 — a mock-deployment stress test, and a regression check on round 1\'s own fixes' },
  { p: 'After round 1\'s fixes were applied, a second, harsher round was run before this package was treated as final: two reviewers who had done round 1 were brought back specifically to verify their own prior findings against the updated files rather than take the fix on faith, and two entirely new reviewers were given only the Deployment Guide and the two scripts and told to literally simulate performing the deployment as a first-time Tier-1 engineer — one on a fresh install, one on an update of an existing install, the second also deliberately injecting four realistic mistakes (a truncated paste, a mistyped input name, a malformed JSON constant, a password saved as the wrong input type) to test whether the Guide\'s own checkpoints would actually catch each one.' },
  { p: 'This round caught real self-inflicted regressions from round 1\'s own edits — most notably a stale present-tense claim in Section 12 item 1 (now corrected above), and the Troubleshooting table still pointing engineers at the old reactive Configuration Element setup instead of the new proactive step 3.12 (now corrected in the Deployment Guide). It also surfaced genuine, previously-undisclosed gaps: the VIP auto-assignment race condition (now Section 12, item 8), the complete absence of any guidance for live F5 objects left behind by a partial failure (now Section 12 item 9 and Section 13.3), a mandatory end-to-end test that never actually exercised the auto-VIP-assignment code path where a malformed JSON constant would detonate (the Deployment Guide\'s Section 11 test now requires it), and a rollback plan that instructed backing up a Secret Input\'s value by screenshot — which is impossible, since Secret values are masked in the UI (both this document and the Guide now say plainly that a Secret\'s plaintext cannot be backed up at all, and must be re-sourced from the client on rollback).' },
  { p: 'The mock-deployment reviewers also flagged that two "mandatory, do not skip" verification steps (the Custom Form binding check and the F5-side device confirmation) both silently depended on human contacts the Deployment Guide never gave the engineer a way to identify. The Guide\'s front matter and checklist now require an F5/network contact to be recorded alongside the escalation contact before an engineer begins.' },
  { pageBreak: true },
];

module.exports = { SEC7_CONFIG, SEC8_BUGFIXES };
